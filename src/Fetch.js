import React from 'react'

export default function Fetch(api) {
    // const listApi = api.map((data) =>
    //   <li>{data.gender}</li>
    // );
    // console.log(listApi)

    return (
        <div>
            this is where my api is going to go
        </div>
    )
}
